"""class OrgRequestPullManager:
    use_in_migrations = True

    @classmethod
    def create_request(cls, user):
        if not user.email.email:
            raise ValueError('User not have email')"""